plugins {
    id 'com.android.library'
}

android {
    namespace 'com.maxcloud.keyboard'

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
}